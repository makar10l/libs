std::string input(std::string question){
    print(question);
    std::string in;
    std::cin >> in;
    return in;
}
int input(std::string question, int xyi){
    print(question);
    int in;
    std::cin >> in;
    return in;
}